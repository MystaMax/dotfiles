# ~/.config/zsh/env.zsh — generated by chezmoi from 1Password. Do NOT edit this target file;
# edit the source template instead: ~/.local/share/chezmoi/dot_config/zsh/private_env.zsh.tmpl
# Only the op:// reference is committed to git; the plaintext key never is.
#
# Consumed by the `firecrawl` MCP server (its config references ${FIRECRAWL_API_KEY}).
# Auto-sourced by ~/.zshrc via the glob over ~/.config/zsh/*.zsh.
export FIRECRAWL_API_KEY="{{ onepasswordRead "op://Private/Firecrawl API Key - GitHub Copilot CLI/credential" }}"
